/**
 * @typedef {Object} TreeNode
 * @property {'file' | 'directory'} type - 노드의 타입
 * @property {string} name - 파일 또는 폴더의 이름
 * @property {string} path - 전체 경로
 * @property {number} level - 트리의 깊이
 * @property {TreeNode[]} [children] - 자식 노드 배열 (폴더인 경우에만)
 */
export {}; // 명시적인 모듈 선언